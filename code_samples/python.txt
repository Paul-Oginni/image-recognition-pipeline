## Lists
  - get the number of items in a list:
    - len([1,2,3])

## General
  - get item's type
    - type(item)

## Functional programming

  - map & lambda funcitons
    items = [1, 2, 3, 4, 5]
    squared = list(map(lambda x: x**2, items))

  - filter
    number_list = range(-5, 5)
    less_than_zero = list(filter(lambda x: x < 0, number_list))
